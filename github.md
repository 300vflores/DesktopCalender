repo: 300vflores/DesktopCalender
branch: main

## Last sync

date: 2026-09-03T00:00:00Z
note: Repository is empty (no commits yet) — nothing imported from upstream. Association recorded so future syncs can diff against it.

### Updated in this project

- Built the desktop planner as a single Design Component (`Workspace.dc.html`).
- Bundled a standalone `index.html` for GitHub Pages (self-contained, no dependencies).
- Two accounts with separate data: Victoria Flores (Web Developer), Anna Flores (Outdoors lady).
- Seasonal calendar header, desk scrapbook, letter messaging, and trash with restore.

## Screen map

| Screen | Built from |
| --- | --- |
| Dashboard | `Workspace.dc.html` (isDashboard branch) |
| Calendar | `Workspace.dc.html` (isCalendar branch, `SEASONS`, `holidayFor`) |
| To-Do | `Workspace.dc.html` (isTodo branch) |
| Tasks | `Workspace.dc.html` (isTasks branch + detail aside) |
| Notes | `Workspace.dc.html` (isNotes branch) |
| Progress board | `Workspace.dc.html` (isProgress branch) |
| My Desk rail | `Workspace.dc.html` (panelOpen branch: message, scrapbook, workload) |
| Deployed site | `index.html` (bundled from `Workspace.dc.html`) |
